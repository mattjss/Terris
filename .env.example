# Frontend (Vite) — leave empty to use bundled static data only
# Dev server proxies /api → http://127.0.0.1:3001 (see vite.config.ts)
VITE_API_URL=/api

# API server (server/index.ts, server/seed.ts)
PORT=3001
DATABASE_URL=postgres://terris:terris@127.0.0.1:5432/terris
# Optional: embedding + AI features
OPENAI_API_KEY=
