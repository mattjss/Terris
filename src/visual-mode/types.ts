/**
 * Visual mode identity — two educational lenses (Atlas vs Explorer), one Terris shell.
 * Runtime state lives in `useGlobeVisualModeStore`; scene lerping uses `globeVisualBlendRef`.
 */
export type { GlobeVisualMode } from '@/state/globeVisualModeStore'
