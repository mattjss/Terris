/**
 * Mode-aware search over the unified mock catalog (and future indexes).
 */
export {
  searchTerrisEntities,
  groupSearchResultsByType,
  type GroupedSearchResults,
} from '@/data/search/searchTerris'
