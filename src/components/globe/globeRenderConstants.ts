/**
 * Central tuning for Earth presentation — adjust here before touching shaders.
 */

/** `OrbitControls.autoRotateSpeed` when Earth scale + auto-rotate is active. */
export const GLOBE_AUTO_ROTATE_SPEED = 0.038

/** Cloud mesh Y drift (rad/s) — independent of camera orbit. */
export const CLOUD_SHELL_DRIFT_Y = 0.012
